// src/pages/HomePage.jsx

import React from 'react';

function HomePage() {
  return (
    <div style={{ padding: '20px', border: '1px solid #ddd', borderRadius: '5px', backgroundColor: '#f9f9f9' }}>
      <h1>🏠 欢迎回到首页！</h1>
      <p>这是一个使用 React Router v6 布局路由的应用。</p>
      <p>你可以通过上方的导航栏切换到 API 接口测试页面。</p>
      
      <div style={{ marginTop: '30px', borderTop: '1px dashed #ccc', paddingTop: '20px' }}>
        <h3>应用功能概览</h3>
        <ul>
          <li>**/login**: 独立页面，无导航栏。</li>
          <li>**/** (当前页): 使用 `MainLayout` 布局。</li>
          <li>**/test**: 使用 `MainLayout` 布局，并渲染 `TestComponent`。</li>
          <li>**/login**: 独立页面，无导航栏。</li>
          <li>**/** (当前页): 使用 `MainLayout` 布局。</li>
          <li>**/test**: 使用 `MainLayout` 布局，并渲染 `TestComponent`。</li>
          <li>**/login**: 独立页面，无导航栏。</li>
          <li>**/** (当前页): 使用 `MainLayout` 布局。</li>
          <li>**/test**: 使用 `MainLayout` 布局，并渲染 `TestComponent`。</li>
          <li>**/login**: 独立页面，无导航栏。</li>
          <li>**/** (当前页): 使用 `MainLayout` 布局。</li>
          <li>**/test**: 使用 `MainLayout` 布局，并渲染 `TestComponent`。</li>
          <li>**/login**: 独立页面，无导航栏。</li>
          <li>**/** (当前页): 使用 `MainLayout` 布局。</li>
          <li>**/test**: 使用 `MainLayout` 布局，并渲染 `TestComponent`。</li>
          <li>**/login**: 独立页面，无导航栏。</li>
          <li>**/** (当前页): 使用 `MainLayout` 布局。</li>
          <li>**/test**: 使用 `MainLayout` 布局，并渲染 `TestComponent`。</li>
          <li>**/login**: 独立页面，无导航栏。</li>
          <li>**/** (当前页): 使用 `MainLayout` 布局。</li>
          <li>**/test**: 使用 `MainLayout` 布局，并渲染 `TestComponent`。</li>
          <li>**/login**: 独立页面，无导航栏。</li>
          <li>**/** (当前页): 使用 `MainLayout` 布局。</li>
          <li>**/test**: 使用 `MainLayout` 布局，并渲染 `TestComponent`。</li>
          <li>**/login**: 独立页面，无导航栏。</li>
          <li>**/** (当前页): 使用 `MainLayout` 布局。</li>
          <li>**/test**: 使用 `MainLayout` 布局，并渲染 `TestComponent`。</li>
          <li>**/login**: 独立页面，无导航栏。</li>
          <li>**/** (当前页): 使用 `MainLayout` 布局。</li>
          <li>**/test**: 使用 `MainLayout` 布局，并渲染 `TestComponent`。</li>
          <li>**/login**: 独立页面，无导航栏。</li>
          <li>**/** (当前页): 使用 `MainLayout` 布局。</li>
          <li>**/test**: 使用 `MainLayout` 布局，并渲染 `TestComponent`。</li>
          <li>**/login**: 独立页面，无导航栏。</li>
          <li>**/** (当前页): 使用 `MainLayout` 布局。</li>
          <li>**/test**: 使用 `MainLayout` 布局，并渲染 `TestComponent`。</li>
        </ul>
      </div>
    </div>
  );
}

export default HomePage;